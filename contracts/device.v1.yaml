openapi: 3.1.0
info:
  title: SyncroBrain Device API (legacy)
  version: 1.0.0
  description: |
    Deprecated as the Cloud Lite contract. Keep Device CRUD compatible;
    new orchestration lives in gateway.v1.yaml on port 13200.
    Do not treat this file as the Pack / demo / alarm surface.
servers:
  - url: http://localhost:13200/api/v1
paths:
  /health:
    get:
      operationId: health
      deprecated: true
      responses:
        "200":
          description: OK — prefer gateway.v1 Health schema
  /devices:
    get:
      operationId: listDevices
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Device list
    post:
      operationId: createDevice
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateDeviceRequest"
      responses:
        "201":
          description: Created
  /devices/{id}:
    get:
      operationId: getDevice
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Device
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    CreateDeviceRequest:
      type: object
      required: [name, protocol]
      properties:
        name: { type: string }
        protocol: { type: string, enum: [mqtt, modbus, http] }
        metadata: { type: object, additionalProperties: true }
